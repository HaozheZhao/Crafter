# Crafter uses a single OpenAI-compatible endpoint (OpenRouter by default).
export OPENROUTER_API_KEY="sk-or-..."
# Optional overrides:
# export OPENROUTER_API_BASE="https://openrouter.ai/api/v1"
# export CRAFTER_CONFIG="configs/default.yaml"
# Editor processing phase only (grounding server):
# export SAM3_SERVER_URL="http://host:port"
